@using test.DTOs;
@model EmpregadosDTO

@{
    ViewBag.Title = "Editar";
}

<form action="/EmpregadosDTO/Alterar" id="formEdicao" method="post">
    <div>
        <input type="hidden" name="ID" id="id-edicao" value="@Model.ID" />
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    CPF
                </label>
            </div>
            <div class="div-input-validatiton">
                <div>
                    <input type="text" class="input" id="cpf-edicao" name="CPF" value="@Model.CPF" onkeyup="mascara('###.###.###-##',this,event,true)" maxlength="14" required />
                </div>
                <div><span id="cpfAlertEdicao" class="text-danger size-text-danger"></span></div>
            </div>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    Empresa
                </label>
            </div>
            <select class="input" id="selected-edicao" name="Empresa" value="@Model.RazaoSocial">
                @foreach (var empresa in ViewBag.Empresa)
                {
                    <option value="@empresa.ID" @Html.Raw(Model.Empresa == empresa.ID.ToString() ? "selected" : "")>@empresa.RazaoSocial</option>
                }
            </select>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    Nome
                </label>
            </div>
            <div class="div-input-validatiton">
                <div>
                    <input type="text" class="input" id="nome-edicao" name="Nome" value="@Model.Nome" required />
                </div>
                <div><span id="nomeAlertEdicao" class="text-danger size-text-danger"></span></div>
            </div>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    RG ou Passaporte
                </label>
            </div>
            <div class="div-input-validatiton">
                <div>
                    <input type="text" class="input" id="rg-passaporte-edicao" name="RGPassaporte" value="@Model.RGPassaporte" onkeyup="mascara('##.###.###-#',this,event,true)" maxlength="12" />
                </div>
            </div>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    Data de Nascimento
                </label>
            </div>
            <div class="div-input-validatiton">
                <div>
                    <input type="date" class="input" id="data-nascimento-edicao" name="DataNascimento" value="@Model.DataNascimento.ToString("yyyy-MM-dd")" required" />
                </div>
                <div><span id="dataNascAlertEdicao" class="text-danger size-text-danger"></span></div>
            </div>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    Ultimo ASO
                </label>
            </div>
            <div class="div-input-validatiton">
                <div>
                    <input type="date" class="input" id="ultimo-aso-edicao" name="UltimoASO" value="@Model.UltimoASO.ToString("yyyy-MM-dd")" required" />
                </div>
            </div>
        </div>
        <div class="div-form mb-3">
            <div>
                <label class="lb-class">
                    Função
                </label>
            </div>
            <select name="NomeFuncao" class="input" id="funcao-edicao">
                @foreach (var funcao in ViewBag.Funcao)
                {
                if (funcao.Ativo)
                {
                    <option value="@funcao.Id" @Html.Raw(Model.NomeFuncao == @funcao.Id.ToString() ? "selected" : "")>@funcao.Nome</option>
                }
                }
            </select>
        </div>
        <div class="div-form mb-3">
            <div>
            <label class="lb-class">
                Setor
            </label>
            </div>
            <select name="NomeSetor" class="input" id="setor-edicao">
                @foreach (var setor in ViewBag.Setor)
                {
                    if (setor.Ativo)
                    {
                        <option value="@setor.Id" @Html.Raw(Model.NomeSetor == @setor.Id.ToString() ? "selected" : "")>@setor.Nome</option>
                    }
                }
            </select>
        </div>
    </div>
</form>


